<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.constraintlayout.utils.widget.ImageFilterView
        android:id="@+id/dialog_imageFilterViewMain2"
        android:layout_width="300dp"
        android:layout_height="250dp"
        android:layout_marginTop="20dp"
        android:background="@color/white_80"
        android:foreground="?android:attr/selectableItemBackground"
        android:scaleType="centerInside"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/dialog_imageFilterViewLock2"
        app:round="50dp"
        app:srcCompat="@drawable/main" />

    <androidx.constraintlayout.utils.widget.ImageFilterView
        android:id="@+id/dialog_imageFilterViewLock2"
        android:layout_width="300dp"
        android:layout_height="250dp"
        android:background="@color/white_80"
        android:foreground="?android:attr/selectableItemBackground"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:round="50dp"
        app:srcCompat="@drawable/lock" />

    <Button
        android:id="@+id/dialog_buttonSet"
        android:layout_width="0dp"
        android:layout_height="80dp"
        android:layout_marginTop="20dp"
        android:backgroundTint="@color/white_50"
        android:stateListAnimator="@null"
        android:text="@string/dialog_setAll"
        android:textColor="@color/red"
        android:textSize="20sp"
        app:cornerRadius="60dp"
        app:layout_constraintEnd_toEndOf="@+id/dialog_imageFilterViewMain2"
        app:layout_constraintStart_toStartOf="@+id/dialog_imageFilterViewMain2"
        app:layout_constraintTop_toBottomOf="@+id/dialog_imageFilterViewMain2" />

</androidx.constraintlayout.widget.ConstraintLayout>